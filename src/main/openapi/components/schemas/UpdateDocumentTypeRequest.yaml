type: object
properties:
  version:
    type: integer
    description: "The lock field for the object."
  code:
    type: string
    description: "The unique code for the document type."
  name:
    type: string
    description: "The display name for the document type."
  description:
    type: string
    description: "A description of the document type."
required: [ version, code, name, description ]