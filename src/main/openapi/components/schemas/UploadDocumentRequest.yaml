type: object
required: [ document ]
properties:
  document:
    allOf:
      - $ref: ./BinaryFile.yaml
    description: The file to upload (JPG).
description: Fields required for document upload via multipart/form-data.
