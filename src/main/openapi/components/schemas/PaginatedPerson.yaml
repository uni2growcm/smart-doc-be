type: object
description: Structure for a paginated list of persons.
properties:
  data:
    type: array
    items:
      $ref: ./Person.yaml
    description: List of persons for the current page.
  metadata:
    $ref: ./PageInfo.yaml