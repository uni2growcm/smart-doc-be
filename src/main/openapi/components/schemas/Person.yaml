allOf:
  - $ref: ./Base.yaml
  - type: object
    description: Person identification data.
    properties:
      name:
        type: string
        description: Person's name (available when integrated with OH).
      pid:
        type: string
        description: The patient external identifier.
      email:
        type: string
        format: email
        description: Contact email.
      phoneNumber:
        type: string
        description: Primary phone number.
      gender:
        $ref: ./Gender.yaml
      status:
        $ref: ./Status.yaml
    required: [ name, pid, status ]
